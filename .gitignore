# dependencies
node_modules/

# build output
dist/

# data — dataset.csv IS committed (so deploy can build the DB without Python);
# the SQLite DB and redis dumps are generated and stay ignored.
backend/data/*.db
backend/data/*.db-*
backend/data/redis/

# logs & os
*.log
.DS_Store
_vivaprep_DO_NOT_SUBMIT/
